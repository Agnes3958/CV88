// helper: move IMG_1559.JPG to assets/profile.jpg by creating a copy and updating index.html references
// Note: GitHub API doesn't provide a direct 'move' file endpoint; we implement by creating new file with same blob and removing the old one if needed.
